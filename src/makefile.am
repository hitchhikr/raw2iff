CC = m68k-amigaos-gcc
RM = rm
CFLAGS = -Os -noixemul -D__BIG_ENDIAN__ -D__AMIGA__

.c.o:
	$(CC) $(CFLAGS) -o $@ -c $<

files = raw2iff.o

all: raw2iff.am

raw2iff.am: $(files)
	$(CC) $(CFLAGS) -s -o ../$@ $(files) $(LIBS)

clean:
	-$(RM) $(files)
