CC = m68k-amigaos-gcc
RM = rm
STRIP = m68k-amigaos-strip
CFLAGS = -Os -noixemul -D__BIG_ENDIAN__ -D__AMIGA__

.c.o:
	$(CC) $(CFLAGS) -o $@ -c $<

files = raw2iff.o

all: raw2iff

raw2iff: $(files)
	$(CC) $(CFLAGS) -o $@ $(files) $(LIBS)
	$(STRIP) $@

clean:
	-$(RM) $(files)
